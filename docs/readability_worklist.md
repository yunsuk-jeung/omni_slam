# Readability Worklist (Phase 2 analysis output)

51 files, 148 findings. All risk none/low.

---

## TODO — later cleanup sweep (comments & logs)

Goal: strip noise. Remove **change-rationale / "why I changed it this way" /
defensive** comments and **useless logs**. Keep a comment only when it explains
genuinely non-obvious *code intent* (an algorithm's name/steps, an
order-significance invariant) — not the reason an edit was made.

**Remove (change-rationale / defensive — the kind the user flagged):**
- `utils/logger.hpp` — "Construct exactly once even under concurrent…",
  "localtime_r is used instead of std::localtime because…"
- `apps/run_svio.cpp`, `apps/run_svo.cpp` — "Cadence for polling…",
  "Substitute another EuRoC sequence here…"
- `odometry/stereo_vo.cpp`, `stereo_vio.cpp` — "Large sentinel so the first
  tracked frame…", "Inverse-distance upper bound… (~1/3 m)", "Guards the
  reciprocal-distance sum against division by zero…", "The two most recent
  keyframes are always kept…"
- `optimizer/cost_function.hpp` — "Minimum camera-frame depth (z) below which…"
- `optimizer/marginalizer.hpp` — "Guard against a stale prior…"
- `optimizer/geometry.hpp` — "Degenerate: point at infinity…" / "…camera center"
- `optimizer/vo_estimator.cpp` — "Schur complement dims: r = kept…"
- `imu_preintegration.cpp/.hpp` — "Diagonal regularization keeps LDLT stable…",
  "Overwrites only bias_acc_/bias_gyr_ (unlike reset()…)", "Move the buffer
  aside; reset() will clear it…", buffer/repropagate note
- `config/svio_config.cpp` — "Each IMU field falls back in order…"
- `database/map_point.hpp` — "Order is significant… Do not reorder." (keep? it
  guards a real reorder bug — decide during sweep)
- `utils/timer.hpp` — "First-seen order for report_all()…", "No samples -> 0…"
- `utils/ceres_utils.hpp` — reset-cursor / merge-join / counting-sort notes
  (borderline: algorithm intent — likely KEEP)

Regenerate the full added-comment list:
`git diff 17da040 -- cpp apps | grep -nE '^\+\s*//'`

**Remove useless logs (per the debug-removal direction):**
- `odometry/stereo_vo.cpp`, `stereo_vio.cpp` — `LogD(...)` traces (connected
  ratio, init counts, "stereoVO initialized …")
- keep genuine WARN/ERROR (`Prior skipped`, `Skip …`, `eigen ldlt failed`) and
  the `Statistics` profiling facility unless told otherwise
- (already done) gauge-shift/cost-breakdown `[wopt]` instrumentation removed

---

## utils  (8 files, 17 findings)

- **cpp/src/utils/ceres_utils.hpp** (163 loc)
    - _risk_: Both functions are order-sensitive sparse-matrix algorithms: the counting-sort transpose (histogram at 31-37, prefix-sum at 39-41, scatter at 43-56, shift-back at 58-61) must keep those four passes in exactly this sequence, and the merge-join in create_hessian_from_crs_matrix (100-138) relies on crsJt's per-row column indices already being ascending (inherited from J's row-sorted CRS layout) — any refactor must not reorder these passes or change iteration bounds. H(i,j) entries left untouched by the two `continue` early-outs (104-105, 116-117) are relied upon to still be 0 from H.setZero() at line 94; a naming/structure edit must not disturb that invariant.
    - [comment/none/trivial] L27: Add a short comment above line 27 naming the technique, e.g. "// CSR->CSC transpose via counting sort: histogram column counts, prefix-sum into row offsets, scatter values, then shift offsets back."
    - [comment/none/trivial] L100: Add a comment explaining this is a sparse dot-product via merge-join over ascending column indices, and that skipped (i,j) entries stay 0 because H was zeroed via H.setZero() above.
    - [naming/none/small] L91: Rename to match this file's snake_case convention used in transpose_crs_matrix (e.g. num_rows, i_begin/i_end, j_begin/j_end) for consistency.
    - [local-structure/none/trivial] L108: Drop the redundant reassignment of i_colIdx_end inside the j-loop; keep only the i_colIdx = rows[i]; reset, since only i_colIdx is mutated by the inner while-loop merge.
    - [naming/none/trivial] L72: Rename parameter res to residuals for clarity at call sites/readers (matches JtR's implied meaning of J-transpose-times-residual).
- **cpp/src/utils/concurrent_queue.hpp** (86 loc) — clean
- **cpp/src/utils/eigen_utils.hpp** (40 loc)
    - _risk_: tangent_basis() is likely called from cost-function/Jacobian code (e.g. bearing residuals); any rename must stay parameter-name-only (no signature/order change) and the 0.9 threshold and cross-product order must remain byte-for-byte identical since it affects which axis is picked as the pivot and the resulting basis sign/orientation.
    - [magic-number/none/trivial] L30: Introduce a named constexpr, e.g. `constexpr double kNearParallelDotThreshold = 0.9;` declared just above the function (or at namespace scope), and use it in the comparison. Same value, just documents why 0.9 was chosen (avoiding near-degenerate cross product when f is close to the Z axis).
    - [comment/none/trivial] L25: Add a brief why-comment above the function: builds an orthonormal basis (b1,b2) spanning the plane orthogonal to f, used e.g. for bearing/reprojection residual parameterization; picks UnitY as pivot when f is nearly parallel to UnitZ to avoid a degenerate cross product.
    - [naming/none/trivial] L29: Rename local `a` to something like `pivot_axis` (and correspondingly reference it below) to make its role as the degeneracy-avoidance pivot axis clear; keep b1/b2 or rename to tangent1/tangent2 for symmetry.
- **cpp/src/utils/fmt_eigen.hpp** (62 loc)
    - _risk_: No numerical/ordering logic here (pure string formatting), so behavior-preservation risk for any change is inherently low; the only real hazard is accidentally altering the literal spacing/dash-count strings that define on-screen alignment, which would change rendered output byte-for-byte even though it wouldn't affect any numeric result.
    - [naming/none/trivial] L19: Rename to isColVec / isRowVec / isVector to match the camelCase convention used by eigenPrecision in the same function (and the rest of the codebase's style).
    - [comment/none/trivial] L38: Add a short comment explaining why column vectors are transposed before formatting (so a Nx1 column prints as a single horizontal line using the vector IOFormat, instead of N stacked single-element rows).
    - [magic-number/none/small] L23: Extract these two literals into named constexpr std::string_view (or const char*) constants, e.g. matrixRowIndent and matrixHeaderDivider, with a brief comment noting they exist purely to visually align/separate matrix rows in the console output. Keep the exact same characters/lengths.
- **cpp/src/utils/logger.hpp** (133 loc)
    - _risk_: Preserve sink construction/registration order in init() (console_sink then file_sink, both passed to the vector in that order) since it affects observable log output ordering/formatting. Preserve the pattern string "[%Y-%m-%d %H:%M:%S.%e] [%^%l%$] %v" and filename format "%Y%m%d_%H%M%S" verbatim. Do not touch the sizeof...(args)==0 vs vformat branching in error() — the branch exists specifically to avoid re-interpreting a plain string as a format string.
    - [dead-code/low/trivial] L132: This macro is defined but not referenced anywhere in the codebase (verified via repo-wide grep) — it is obsolete debugging scaffolding rather than active debug output. Per policy, truly obsolete scaffolding should be removed rather than kept or wrapped; if it is still wanted for ad-hoc use, gate it behind a compile-time DEBUG flag and give it a clearer message than the placeholder "THIS Line is for debugging".
    - [comment/none/trivial] L105: Add a brief comment above the function explaining its purpose/why it exists: it strips the directory portion of __FILE__ (as passed by the LogE macro) so error logs show a concise relative filename instead of an absolute compiler-supplied path.
- **cpp/src/utils/omni_assert.hpp** (57 loc) — clean
- **cpp/src/utils/sophus_utils.hpp** (31 loc) — clean
- **cpp/src/utils/timer.hpp** (163 loc)
    - _risk_: Statistics is a static/global registry (Meyer's singletons via statistics()/order()); any refactor must preserve the exact insertion-order tracking in order() (used by report_all()) separately from the std::map's sorted iteration order, and must not change when/how emplace vs. find is invoked relative to push_back, since that ordering is what report_all() relies on for output order.
    - [comment/none/trivial] L108: Add a short 'why' comment noting that this vector tracks first-seen insertion order separately from statistics() because std::map iterates keys in sorted (alphabetical) order, and report_all() wants timers listed in the order they were first used.
    - [helper-extraction/none/small] L133: Extract a small local lambda, e.g. `auto zero_if_empty = [&](double v) { return e.call_count == 0 ? 0.0 : v; };`, and use it for mean/min/max to name the repeated 'no samples yet -> report 0' rule once instead of three times. Same values computed in the same order, just deduplicated.
    - [naming/none/trivial] L132: Rename `e` to `elem` for consistency with the identically-purposed local `elem` in stop_timer() (line 48), improving readability of this longer function.
    - [naming/none/trivial] L135: Rename locals `min`/`max` to `min_val`/`max_val` (or `display_min`/`display_max`) since `min`/`max` shadow the common std::min/std::max names and could momentarily read as function calls rather than plain values.

## config  (4 files, 10 findings)

- **cpp/src/config/svio_config.cpp** (223 loc)
    - _risk_: The precedence order in each field's read (imu_node alias list checked first, then root-level `config.value("imu_xxx", ...)` as the fallback passed in as `default_value`, then the existing static member value as last resort) is load-bearing for backward-compat config files and must not be reordered or restructured. The clamp/sanitize block (lines 180-199) applies floor values and NaN/negative rejection to specific fields in a specific order right before the debug-print block; the exact threshold values (1e-12, 1e-9, 0.0 bound in sanitize_scale) and which fields get which floor must stay identical if extracted to named constants. Do not touch anything under the anonymous namespace's function bodies beyond the dead-code removal since two of the three helpers are actively used by many call sites below.
    - [dead-code/none/trivial] L69: This helper is never called anywhere in the file (grep confirms no call site); it is truly obsolete scaffolding, not debug output. Remove it, or if it is intended for a near-term future use (e.g. reading g_w overrides), leave a short comment saying so instead of dead code.
    - [comment/none/trivial] L111: Add a short comment above this block explaining the three-tier fallback precedence: (1) alias keys inside the nested "imu" object, (2) the canonical root-level "imu_xxx" key for backward compatibility, (3) the existing static default. This pattern is not obvious from the code alone and is repeated for every IMU field.
    - [local-structure/none/trivial] L118: Introduce `const nlohmann::json& imu = *imu_node;` right after the imu_node lookup (lines 106-109) and use `imu` in place of `*imu_node` in each subsequent call, removing the repeated pointer dereference without altering which object is referenced or read order.
    - [magic-number/none/trivial] L180: Extract the repeated floor value into `constexpr double kMinNoiseParam = 1e-12;` and the dt floor into `constexpr double kMinIntegrationDt = 1e-9;`, keeping the exact same values and per-field application order.
    - [magic-number/none/trivial] L187: This fallback value duplicates the static member's initializer at line 15. Extract a `constexpr double kDefaultMarginalizerInitialBiasWeight = 100.0;` used in both places so the relationship is explicit and the two can't silently drift apart.
- **cpp/src/config/svio_config.hpp** (46 loc) — clean
- **cpp/src/config/svo_config.cpp** (256 loc)
    - _risk_: All arithmetic here is independent scalar clamping/assignment with no shared accumulation, so reordering risk is low, but any touched line must preserve: (1) the exact clamp thresholds/replacement values (e.g. 0.01, 1.0, 1e-6, 1e10, 3) which double as documented defaults matching the static initializers at the top of the file, (2) the order in which cam0 then cam1 are parsed and pushed into the parallel camera_* vectors (index-aligned across vectors, so any refactor of parse_camera_params must keep push_back order per vector identical), and (3) the debug-gated Logger::info block's field order/content, since it's effectively a behavioral log contract some tooling may parse.
    - [dead-code/none/trivial] L170: Remove this second, byte-for-byte duplicate of the clamp already applied at lines 164-166 (it is a guaranteed no-op since bearing_huber_const cannot have changed in between). If a different variable was meant to be clamped here, that should be resolved as a separate bug fix, not folded into this readability pass.
    - [comment/none/trivial] L149: Add a short comment noting that the inner lookup is a deliberate backward-compatibility fallback for a legacy misspelled key ("beraing_cost_scale"), so future readers don't mistake it for an accidental typo and "fix" it.
    - [local-structure/none/trivial] L136: Add braces around the single-statement body to match the brace style used by every other clamp check in this function (e.g. lines 164-194), improving visual consistency.
    - [local-structure/none/trivial] L43: Give parse_camera_params internal linkage (wrap in an anonymous namespace, or mark `static`) since it is a file-local helper not declared in svo_config.hpp and not used elsewhere in the codebase; tightens its scope and documents that it's an implementation detail.
    - [magic-number/none/trivial] L91: Introduce a `constexpr int kSE3MatrixElements = 16;` (4x4 flattened matrix) and use it in both the size check and loop bound, naming the invariant being relied on.
- **cpp/src/config/svo_config.hpp** (45 loc) — clean

## camera_model  (3 files, 7 findings)

- **cpp/src/camera_model/camera_model.cpp** (21 loc)
    - _risk_: No numerical/ordering logic present; file is a pure construction dispatch with no accumulation, matrix ops, or Ceres registration to preserve order for.
- **cpp/src/camera_model/camera_model.hpp** (63 loc)
    - _risk_: File is a header with no executable logic beyond the trivial intrinsics setter, so behavior-preservation risk for any change here is essentially nil as long as public signatures (project/distort/unproject/intrinsics/CameraModelFactory::create) and member layout/initialization order are untouched.
    - [naming/none/trivial] L34: Rename the parameter (e.g. to `intr` or `k`) so it no longer has the same name as the enclosing method `intrinsics(...)`, improving readability at call sites/diffs.
    - [naming/none/trivial] L31: Rename parameter `bearing_` to `bearing` — the trailing underscore is this file's convention for member variables (fx_, fy_, cx_, cy_, cv_K_, cv_D_, has_distortion_), so using it on a parameter is misleading.
- **cpp/src/camera_model/pinhole_radtan.hpp** (139 loc)
    - _risk_: Any touch here must preserve exact arithmetic order in distort() (radial poly then tangential terms into x_dist/y_dist) and in the bearing normalization (r2 -> norm -> norm_inv -> out[0],out[1],out[2] assignment order) since these values feed Ceres residuals/Jacobians downstream; do not reorder operands or change intermediate variable grouping when extracting helpers. The single-point unproject's manual reimplementation of the batch lambda's math must be byte-for-byte equivalent (including using std::sqrt consistently) if merged into a shared helper.
    - [helper-extraction/low/small] L60: Extract the bearing-normalization computation (r2, norm_inv, out[0..2] assignment) into a single private static helper (e.g. normalizeBearing(mx, my, out)) used by both unproject overloads, preserving the exact same operation order/expressions so results are bit-identical.
    - [naming/none/trivial] L35: Rename parameter `nuv` to `n_uv` to match the naming used for the same quantity at call sites in project() (lines 19, 21, 28, 30), improving consistency.
    - [local-structure/none/trivial] L103: Use std::sqrt (as already used in the fill_bearing lambda on line 62) instead of the unqualified sqrt for consistency within the file.
    - [magic-number/none/trivial] L116: Introduce a named constexpr (e.g. constexpr size_t kNumRadTanCoeffs = 4;) for the expected coefficient count (k1,k2,p1,p2), used both in the size check and implicitly by indices 0-3 below, to document why 4 is the threshold.
    - [comment/none/trivial] L46: Add a brief comment noting this implements the Brown-Conrady / OpenCV radial-tangential distortion model (radial term via k1,k2 plus tangential terms via p1,p2) so the formula's intent is clear to readers unfamiliar with the model, without altering the computation.

## database  (4 files, 9 findings)

- **cpp/src/database/frame.cpp** (60 loc)
    - _risk_: The global atomic g_frame_id counter's fetch_add order depends on Frame construction order across threads — do not touch member-init evaluation order. The camera_parameters loop's push_back order defines the cams_ index mapping that cam(cam_idx) relies on elsewhere; preserve iteration order and the skip-on-null-camera behavior exactly.
    - [local-structure/none/trivial] L16: Reorder the initializer list to match the member declaration order in frame.hpp. The compiler already initializes members in declaration order regardless of list order, so this is purely a textual reorder — no runtime change — but it removes the -Wreorder risk/warning and makes the code read as it actually executes.
    - [dead-code/none/trivial] L43: tracking_result_ is a std::unique_ptr whose destructor already releases the resource; the explicit reset() call is redundant (it exists only so the destructor is defined in a translation unit where TrackingResult is a complete type). Consider leaving the destructor body empty, or add a one-line comment stating it is defined here (not defaulted in the header) specifically to allow deleting the still-incomplete-in-the-header TrackingResult.
    - [comment/none/trivial] L35: Add a short comment explaining the intent: cameras beyond the configured camera_T_b_c entries keep the default-constructed (identity) extrinsic. Without this, a reader can't tell whether the size mismatch is an expected fallback or an overlooked edge case.
- **cpp/src/database/frame.hpp** (100 loc)
    - _risk_: Private member declaration order in this header dictates actual C++ initialization order regardless of the mem-initializer-list order used in frame.cpp; do not reorder members without checking frame.cpp's constructor. The FEJ semantics hinge on T_w_b_lin_ being std::optional and unset meaning "still following current estimate" (is_linearized()/twb_lin()/set_lin_true() logic) — any local-structure edit near lines 54-62 must preserve the exact not-set vs set-once behavior. kCamNum, mp_id_to_bearings_, and the twb()/tbc()/twc() naming are used from frame.cpp and other TUs, so any rename here needs a matching edit there even though this review is scoped to this file.
    - [naming/none/small] L81: Rename to cam_num_ (or num_cams_) to match the trailing-underscore convention used by every other private member; the k-prefix conventionally signals a compile-time/global constant, but this is a per-instance runtime value set from images.size(). Requires a matching rename in frame.cpp's mem-initializer list.
    - [naming/none/trivial] L46: Rename the parameter from T_wb to T_w_b to match the underscore-separated style used for T_w_b_ and T_b_cs_ elsewhere in the class; purely local to this inline definition, no signature/ABI effect.
    - [comment/none/trivial] L96: Add a short comment noting the shape: outer vector is indexed by cam_idx, inner map is map-point id -> bearing vector observed by that camera. The member/accessor names don't make clear that the outer container is per-camera.
- **cpp/src/database/map_point.cpp** (23 loc) — clean
- **cpp/src/database/map_point.hpp** (52 loc)
    - _risk_: Status is compared with relational operators elsewhere (e.g. `mp->status() < MapPoint::Status::TRACKING` in vio_estimator.cpp/vo_estimator.cpp/stereo_vo.cpp/stereo_vio.cpp), so the enum's declaration order is load-bearing — do not reorder NONE/TRACKING or otherwise touch the enum's underlying values when adding documentation. bearing_/inv_dist_/host_frame_cam_id_ form the inverse-depth state used by the optimizer (Ceres parameter blocks live on these), distinct from frame_cam_id_to_bearing_ which holds per-observation bearing vectors for reprojection residuals — any comment/rename must not blur or merge these two concepts, and no field layout/type/order may change since EIGEN_MAKE_ALIGNED_OPERATOR_NEW and Ceres param-block pointers may depend on member addresses.
    - [naming/none/trivial] L24: Rename the parameter from `uv` to `bearing` (matching the definition in map_point.cpp, which already names it `bearing`, and matching the member it populates, frame_cam_id_to_bearing_). The current name `uv` misleadingly suggests raw pixel/image coordinates when the stored/passed value is actually a 3D bearing vector.
    - [comment/none/trivial] L14: Add a short comment noting that call sites compare Status with relational operators (e.g. `status() < Status::TRACKING`), so the declared order of the enumerators is significant and must not be changed casually when adding new states.
    - [comment/none/trivial] L44: Add a brief comment explaining these three fields together form the point's inverse-depth parameterization anchored at host_frame_cam_id_ (bearing_ is the ray direction in the host frame, inv_dist_ the inverse distance along it), to distinguish this state from the per-observation bearings kept in frame_cam_id_to_bearing_ below.

## feature_tracking  (3 files, 6 findings)

- **cpp/src/feature_tracking/optical_flow.cpp** (248 loc)
    - _risk_: Any touch to run_forward_backward_optical_flow must preserve the exact order of the two calcOpticalFlowPyrLK calls (forward then backward) and the three sequential status-rejection checks (status/reverse_status -> in-image -> squared-distance threshold), since later checks assume earlier ones already zeroed status[i]. In process(), the reserve() calls, prepare_images_and_pyramids -> track_mono -> detect_features -> track_stereo -> prev_frame_ assignment ordering must not be reordered (detect_features must run after track_mono since it reads curr_uvs already populated by track_mono; prev_frame_ must be updated last).
    - [naming/none/trivial] L58: Rename to diff (it's a displacement vector, not a distance) and dist_norm_sq (file otherwise uses snake_case throughout, e.g. track_count, prev_left_size); distNormSq is the only camelCase identifier in the file.
    - [comment/none/trivial] L58: Add a short why-comment noting that optical_flow_dist_threshold is compared against the squared forward-backward reprojection error (not a raw pixel distance), so future readers/config editors know the units expected when tuning this value.
    - [local-structure/none/small] L176: Introduce a single local `const cv::Mat& left_image = curr_frame->image(kLeftCam);` at the top of detect_features and use left_image.cols/.rows/(roi) everywhere below, removing the five repeated curr_frame->image(kLeftCam) lookups (lines 176, 177, 193, 195, 203) without changing any read order or values.
    - [comment/none/trivial] L236: Add a why-comment explaining the reserve-size heuristic: half of the previous frame's tracked left-camera feature count (as an estimate of how many will survive the forward-backward check) plus one new detection per grid cell. As written the >>1 and formula intent aren't self-evident.
- **cpp/src/feature_tracking/optical_flow.hpp** (43 loc) — clean
- **cpp/src/feature_tracking/tracking_result.hpp** (56 loc)
    - _risk_: No numerical/Ceres/marginalization logic here; it is a plain data holder, so behavior-preservation risk for any edit is inherently low. All cam_idx-indexed accessors are unchecked (rely on caller keeping cam_idx < kCamNum) — worth noting only as a precondition, not fixing.
    - [local-structure/none/trivial] L31: Drop the redundant `idx` alias and use `cam_idx` directly (`ids_[cam_idx].reserve(size); uvs_[cam_idx].reserve(size);`) — it adds no clarity over the parameter name it copies.
    - [comment/none/trivial] L41: Add a brief comment on the class (or these accessors) noting the precondition that cam_idx must be < kCamNum, since none of the indexed accessors bounds-check it.

## device  (7 files, 7 findings)

- **cpp/src/device/dataset_simulator.hpp** (179 loc)
    - _risk_: The two feed_* loops each perform: (1) first-iteration start_ts/start_time capture, (2) speed-scaled sleep_until, (3) a post-sleep terminate_ recheck, (4) callback invocation — in that exact order per iteration. Any helper extraction must preserve this per-iteration ordering (init-before-wait, wait-before-terminate-recheck, recheck-before-callback) and must not change how dt_ns/speed_ is computed/cast, since that affects real playback timing and could change when/whether frames are dropped near stop().
    - [helper-extraction/low/small] L76: extract a private helper, e.g. `void wait_for_playback_time(std::chrono::steady_clock::time_point start_time, int64_t start_ts, int64_t ts_ns) const`, computing dt_ns/wait_ns/target and calling sleep_until exactly as today; call it from both loops in the same place
    - [helper-extraction/low/small] L64: extract a small helper that takes references to initialized/start_ts/start_time plus the new timestamp and sets them on first call, reused by both loops
    - [helper-extraction/low/small] L100: extract into a private helper `CameraParameter build_camera_parameter(size_t i) const` so the loop body reads as 'build params for each image' rather than mixing frame-timing and config-lookup concerns
    - [magic-number/none/trivial] L109: introduce `static constexpr size_t kIntrinsicsCount = 4;` and use it here and in the `{intr[0], intr[1], intr[2], intr[3]}` construction on line 111
    - [magic-number/none/trivial] L120: introduce `static constexpr size_t kResolutionCount = 2;` and use it here
    - [comment/none/trivial] L78: add a brief comment noting wait_ns scales the recorded inter-frame delay by speed_ to support faster/slower-than-recorded playback
- **cpp/src/device/dataset_types.hpp** (26 loc) — clean
- **cpp/src/device/device_interface.hpp** (33 loc) — clean
- **cpp/src/device/euroc_loader.cpp** (440 loc) — clean
- **cpp/src/device/euroc_loader.hpp** (75 loc) — clean
- **cpp/src/device/vio_loader.cpp** (88 loc)
    - _risk_: No FEJ/marginalization/Ceres ordering concerns in this file at all — it is pure filesystem-probing and factory dispatch, so the usual numerical-order landmines do not apply here. The only sequencing that matters is that is_euroc_dataset's mav0 existence check must short-circuit before checking cam0/imu0 subpaths (already structured correctly with an early return).
    - [dead-code/none/trivial] L61: This is prospective scaffolding rather than obsolete code, so per policy it should not simply be deleted; if kept, reduce it to a single plain-text TODO comment (e.g. "// TODO: add KITTI/other dataset detection here") rather than commented-out pseudo-code with a stale function name (IsKittiDataset does not match this file's snake_case free-function naming), to avoid it being copy-pasted verbatim later.
- **cpp/src/device/vio_loader.hpp** (73 loc) — clean

## optimizer  (9 files, 44 findings)

- **cpp/src/optimizer/cost_function.hpp** (1062 loc)
    - _risk_: Any touch here must preserve: (1) the exact order sqrt_information_ is applied to residuals/jacobians (right-multiplication after raw residual/jacobian assembly, not fused differently); (2) the FEJ branching (T_w_b_*_lin_ / lin_ optional) that picks the linearization pose for the chain-rule term while residuals/Jr(so3) always use the current iterate — do not let a refactor accidentally hoist a "current vs lin" selection out of its branch; (3) the degeneracy guards' exact thresholds (b_norm <= 0.0, inv_dist <= 0.0, isfinite(inv_dist), p_c.z()/p_c_obs.z() <= 1e-6) and their early-return semantics (residuals zeroed, jacobians zeroed, return true — Ceres treats this as a valid but zero-contribution evaluation, not a failure); (4) parameter block index/order in the SizedCostFunction template args and Evaluate's params[]/jacobians[] indexing, which is tied to how these functors are registered elsewhere.
    - [magic-number/none/trivial] L55: introduce a single file-scope `constexpr double kMinValidDepth = 1e-6;` and use it at all six call sites instead of the bare literal
    - [helper-extraction/low/small] L278: factor the common body into a local lambda, e.g. `auto reject = [&]() { residuals[0]=0.0; residuals[1]=0.0; zero_jacobians(jacobians); return true; };`, and replace each branch with `return reject();` — same statements executed in the same order per branch
    - [helper-extraction/low/small] L450: apply the same local-lambda extraction as in BearingCost::Evaluate (a class-local lambda, not shared across classes, to avoid any header/API change)
    - [magic-number/low/medium] L758: add named constexpr offsets local to each type, e.g. kResIdxPos=0, kResIdxRot=3, kResIdxVel=6, kResIdxBiasAcc=9, kResIdxBiasGyr=12, and use them in the .segment<3>()/.block<3,3>() calls in place of the literals
    - [naming/none/trivial] L172: rename to something self-describing like `tangent_basis_d` (mirroring how `B` is used for the cast result)
- **cpp/src/optimizer/geometry.hpp** (59 loc)
    - _risk_: All Eigen expressions here are order-sensitive (row assignments into A, SVD column extraction, sign-flip of bearing/inv_dist). Any refactor must preserve the exact row order of matrix A (rows 0-3), the same SVD matrix/column (matrixV().col(3)), and the same order of the two epsilon-guard early-returns relative to the rest of the computation. Do not reorder the r0.dot(bearing) sign-flip relative to inv_dist's flip since they must stay coupled.
    - [naming/none/trivial] L24: Rename mySVD to svd (the "my" prefix is Hungarian-notation-style noise inconsistent with the rest of the file's naming) e.g. `Eigen::JacobiSVD<Eigen::Matrix<double, 4, 4>> svd(A, Eigen::ComputeFullV);` and update the one use on the next line to svd.matrixV().
    - [local-structure/none/trivial] L18: Drop the redundant runtime-size constructor arguments on this fixed-size (compile-time 4x4) Eigen matrix: `Eigen::Matrix<double, 4, 4> A;`. The (4, 4) args are inert for a fixed-size type and read as leftover copy-paste from a dynamic-size Matrix construction, which is confusing.
    - [helper-extraction/none/trivial] L29: Extract a local static/lambda helper, e.g. `auto invalidResult = []{ return Eigen::Vector4d::Constant(std::numeric_limits<double>::quiet_NaN()); };`, and call it from both guard blocks (`return invalidResult();`). Keeps both independent early-return sites and their distinct conditions untouched, just names/dedupes the shared return expression.
    - [comment/none/trivial] L29: Add a brief why-comment above this guard (and the analogous one at line 37) explaining what physical condition is being rejected, e.g. "// Degenerate triangulation: point at infinity (head_norm~0) or homogeneous coordinate collapse (w~0)" and for line 37 "// Degenerate triangulation: point coincides with camera center (dist~0)". Currently a reader has to reverse-engineer the DLT math to know why these are invalid cases.
    - [local-structure/none/trivial] L14: Split the combined declaration into two separate lines (`Eigen::Matrix<double, 3, 4> P1;` / `Eigen::Matrix<double, 3, 4> P2;`) directly above their respective initializations (setIdentity() and matrix3x4() assignment) for clarity, since P1 and P2 are set in two different ways immediately after.
- **cpp/src/optimizer/marginalizer.hpp** (115 loc)
    - _risk_: Preserve the exact block iteration order used to build dx and to fill jacobians, the pose-block special case versus the generic segment-wise difference for non-pose blocks, and the order of operations in the jacobian ambient-chart conversion; do not change kPoseBlockSize.
    - [comment/none/trivial] L49: Add a brief why-comment explaining this guards against a stale/mismatched linearization if block sizes or J_/x0_ ever get out of sync with the registered parameter blocks.
    - [magic-number/none/small] L68: Introduce a static constexpr int kSo3TangentSize = 3 (== kPoseBlockSize/2) next to kPoseBlockSize and use it in place of the repeated literal 3 to make the translation/rotation split explicit.
- **cpp/src/optimizer/parameterization.hpp** (186 loc)
    - _risk_: Any touched line must preserve exact Plus/Minus/Jacobian arithmetic order (Ceres relies on these matching each other and on RowMajor jacobian layout). Do not reorder Eigen::Map construction relative to use, and do not alter the sin_theta<1e-10 branch's numeric behavior — only naming it is safe.
    - [magic-number/none/trivial] L155: Introduce a named constexpr, e.g. `static constexpr double kSinThetaEpsilon = 1e-10;` near the top of BearingTangentManifold, and use it here: `if (sin_theta < kSinThetaEpsilon) {`. Same threshold value, purely a name for the small-angle cutoff.
    - [comment/none/trivial] L96: Remove the stray empty `//` comment line between AmbientSize() and the TangentSize() doc comment; it appears to be a leftover formatting artifact and the following `// δ ∈ R^2` already documents TangentSize().
    - [magic-number/none/trivial] L95: For consistency with SE3BoxplusManifold (which defines `kAmbientSize`/`kTangentSize` constexprs), add `static constexpr int kAmbientSize = 3;` and `static constexpr int kTangentSize = 2;` to BearingTangentManifold and return those from AmbientSize()/TangentSize() instead of bare literals. Same values, same behavior.
    - [helper-extraction/none/small] L106: This exact pattern (map a raw double* as Vector3d and normalize) is repeated verbatim at lines 106, 132, 146, 147, and 173. Extract a small private static helper, e.g. `static Eigen::Vector3d NormalizedBearing(const double* p) { return Eigen::Map<const Eigen::Vector3d>(p).normalized(); }`, and call it at each site (`f = NormalizedBearing(x);`, `fx = NormalizedBearing(x);`, `fy = NormalizedBearing(y);`). Pure syntactic dedup — identical computation, identical call order, no behavior change.
- **cpp/src/optimizer/relative_pose.hpp** (52 loc)
    - _risk_: Any restructuring must preserve: (1) the exact order of operations feeding d_rel_d_h vs d_rel_d_t (each is gated independently by its own nullptr check, and code must not start computing shared quantities unconditionally, which would change when work happens even though it wouldn't change any numeric result); (2) the exact matrix expressions/signs in each corner block (topLeftCorner, topRightCorner, bottomRightCorner) since these encode a specific FEJ-compatible linearization; (3) that T_t_h itself is always computed and returned regardless of which Jacobian pointers are null.
    - [naming/none/trivial] L39: Rename `u` to a name following the file's existing R_<to>_<from>/T_<to>_<from> convention, e.g. `p_h_in_tb` (position of camera-h origin, expressed in the t-body frame), so it reads consistently with R_ct_w, R_cb_t, R_wb_h nearby.
    - [helper-extraction/low/small] L27: Hoist a single `R_ct_w` computation to just before the two `if` blocks, guarded by `if (d_rel_d_h || d_rel_d_t)` (or computed once and reused by both branches), removing the redundant duplicate computation while keeping it uncomputed when neither pointer is passed.
    - [comment/none/trivial] L31: Add a short "why" comment above lines 31-32 and 43 noting these are lever-arm (translation-rotation coupling) terms from differentiating the camera origin's position w.r.t. the body's rotation increment, since the hat(...) usage isn't self-explanatory from the top-of-file comment alone.
- **cpp/src/optimizer/vio_estimator.cpp** (1211 loc)
    - _risk_: Several sections are explicitly order-sensitive and must not be reordered even incidentally: (1) WindowBlocks pose/inertial registration order in optimize_window/marginalize feeds Ceres parameter block pointers that must stay stable; (2) evaluate_cost_breakdown relies on residual blocks being stored in insertion order (imu | prior | bearing) — any refactor that changes when add_imu_factor / add_marginalization_prior_if_available / add_bearing_residuals_for_map_point are called relative to each other would silently corrupt the debug cost split; (3) the marginalize() eval_opts.parameter_blocks construction (lines ~979-1020) is explicitly built as [marginal | remain] and the Schur complement partition (H.block(0,0,m,m) vs H.block(m,m,r,r)) depends on that exact concatenation order, not on registration order — any helper-extraction there must preserve the marginal-then-remain, poses-then-inertial call sequence; (4) Statistics::start_timer/stop_timer("marginalize saes"/"saes2"/"eval") pairs must remain matched on every exit path; (5) FEJ correctness depends on make_imu_lin_state's frozen/current selection and on set_lin_true() only running when SVIOConfig::enable_fej is set — do not touch that gate.
    - [dead-code/none/small] L29: join_ids is only ever referenced from disabled LogI calls inside commented-out blocks (around lines 418-423, 589-599, 721-734, 850-852, 941-950, 1197-1207) — there is no live call site. Per the debug-output policy, don't delete this tracing; instead consolidate it into one compile-time-guarded debug logger helper (e.g. a small function or macro gated on a debug flag) that the marginalize()/optimize_window() call sites invoke, so the trace code compiles, is exercised, and stops rotting as scattered comments.
    - [dead-code/none/small] L903: These five counters are computed and incremented throughout the loop but are read only by the disabled LogI at lines 941-950 — right now they are pure overhead with no observable effect. Fold the counting and the log call together into the same compile-time-guarded debug logger proposed for join_ids, so the bookkeeping is either compiled out in release or genuinely surfaced.
    - [comment/low/small] L519: This fires unconditionally on every clear_prior() call, from any of the 7 distinct guard sites in marginalize() (lines 1028, 1039, 1052, 1077, 1126, 1150, 1155), with no indication of which check failed. The sibling VOEstimator::clear_prior() in vo_estimator.cpp has no equivalent line at all, which suggests this may be a leftover debug statement. Consider a message that names the actual failure (or thread a reason string from each call site) so the log is actionable instead of a generic, always-identical string.
    - [magic-number/none/trivial] L1082: Hoist a single named constant (e.g. kEigenvalueFloorEps = 1e-8) next to the other file-scope constants (kPoseSize, etc.) and reference it from both eigenvalue-thresholding blocks instead of re-declaring the same literal twice in two different local scopes.
    - [helper-extraction/none/small] L817: Extract the shared body into a lambda, e.g. ensure_remain_pose_block(uint64_t id), and call it once per id (still iterating marginal_inertial_ids fully, then remain_inertial_ids, in the same order) to remove the duplicated condition without changing which ids get inserted or when.
    - [helper-extraction/low/small] L979: Extract push_pose_params(const std::set<uint64_t>& ids) and push_inertial_params(const std::set<uint64_t>& ids) lambdas capturing frame_id_to_index/pose_params and inertial_id_to_index/velocity_params/bias_acc_params/bias_gyr_params respectively, then call them in the same marginal-then-remain, poses-then-inertial sequence already present. This removes ~20 duplicated lines while leaving the explicit [marginal | remain] push order into eval_opts.parameter_blocks (which the Schur complement partition depends on) untouched.
    - [magic-number/none/trivial] L476: Extract to a named constexpr (e.g. kSingleFrameNumThreads = 2) near the other constants at the top of the file — unlike optimize_window's options.num_threads (SVOConfig::window_num_threads), this value isn't sourced from a config struct and reads as an unexplained literal.
    - [magic-number/none/trivial] L674: Name these two thresholds (e.g. kGaugeShiftMeanThresholdM = 0.02, kGaugeShiftSpreadRatio = 0.5) so the rigid-window-shift heuristic's tuning is self-documenting at its point of definition instead of embedded in a boolean expression.
    - [comment/none/trivial] L364: Add a short comment (or named constants) stating that 2 means bias-only blocks [acc, gyr] and 3 means [velocity, acc, gyr] per inertial state — the meaning is only inferable by cross-referencing the push logic at line 411 (`if (per_inertial_block_count == 3) push velocity`).
    - [comment/none/trivial] L495: Add a why-comment: SE3BoxplusManifold's tangent layout is [t(3), so3(3)], so the top-left 3x3 corner is the translation-prior weight and index 5 is the so3 z-component, which for a near-identity initial pose is effectively the (gravity-unobservable) yaw direction — that's why only that single diagonal entry gets a weight instead of the full 3x3 rotation block.
    - [comment/none/trivial] L138: Both call sites of add_imu_factor in this file (lines 567 and 922) always pass ImuCostType::kAnalytic, so the kAutoDiff branch (lines 225-230) is currently unreachable. Add a brief comment noting it's kept as a manual verification path for cross-checking the analytic IMU Jacobian against autodiff, so a future reader doesn't mistake it for accidental dead code.
- **cpp/src/optimizer/vio_estimator.hpp** (48 loc) — clean
- **cpp/src/optimizer/vo_estimator.cpp** (558 loc)
    - _risk_: This file is numerically sensitive: the marginalization block (lines ~456-556) depends on exact Hessian/Jacobian block ordering (Amm/Amr/Arm, m/r split) and eigenvalue-based pseudo-inverse thresholding (eps = 1e-8) - any reordering of the Schur-complement arithmetic or of parameter-block registration (which fixes column ordering in the CRS Jacobian) would silently change results. The two near-duplicate residual-construction loops in optimize_window() and marginalize() differ subtly (loss-function allocation placement, and presence/absence of an inv_dist validity check) - do NOT merge them into a shared helper in a readability pass, since replicating those differences faithfully is error-prone and any accidental normalization would change optimizer behavior.
    - [dead-code/low/trivial] L57: Unused variable: mp_id_to_uv is bound here but never referenced again - the identical call frame->observation(kCamIdx) is re-issued and bound to mp_id_to_bearing at line 67, which is the one actually used in the loop at line 69. Remove this line entirely to eliminate the redundant call and dead reference.
    - [dead-code/low/trivial] L58: T_w_b is declared but never used anywhere else in optimize_single_frame(). Remove the unused binding.
    - [dead-code/low/trivial] L77: Twc is computed but never used - the very next line (80) recomputes the identical expression f0->twc(kCamIdx) into Twc0, which is the value actually consumed at line 86. Delete line 77 to remove the duplicate call and dead variable.
    - [dead-code/none/trivial] L95: Commented-out alternate cost-function construction (an AutoDiff variant that isn't used). This is obsolete scaffolding rather than useful debug output, so per policy it can be removed outright rather than routed through a debug logger.
    - [comment/none/trivial] L104: The inline comment claims no robust loss is applied, but loss is a ceres::HuberLoss constructed two lines above - the comment is stale/misleading. Remove it or replace with an accurate note, after confirming with the author which of comment-vs-code reflects the intended design.
    - [dead-code/none/small] L117: Commented-out debug logging of the solver summary. Per policy this is useful debug output, not obsolete scaffolding - move it behind a compile-time-guarded debug logger helper instead of leaving it commented out.
    - [magic-number/none/trivial] L111: Hardcoded thread count for the single-frame solver, inconsistent with optimize_window() which sources its thread count from SVOConfig::window_num_threads (line 274). Extract to a named constexpr, e.g. static constexpr int kSingleFrameNumThreads = 2; near the top-of-file constants, and use it here.
    - [local-structure/none/trivial] L330: std::set::insert is already a no-op when the key is present, so the surrounding existence check is redundant. Simplify to a single remain_frame_ids.insert(frame_cam_id.frame_id); - same resulting set contents, iteration order unaffected since std::set is always sorted by key regardless of insertion order.
    - [magic-number/none/small] L495: The same numerical-rank/eigenvalue cutoff constant (1e-8) is declared twice as separate local eps variables within the same function. Hoist a single named constant (e.g. kEigenvalueRankEps) once near the top of marginalize() and reuse it at both call sites.
    - [comment/none/trivial] L471: The single-letter dimensions r (kept/remaining state dim) and m (marginalized state dim) driving the Schur-complement block extraction below are not self-explanatory. Add a short comment above this block naming the convention to help future readers without touching any of the block-extraction arithmetic.
- **cpp/src/optimizer/vo_estimator.hpp** (41 loc)
    - _risk_: This is a declarations-only header; parameter names carry no linkage/ABI meaning in C++, so renaming them here (and optionally matching them in vo_estimator.cpp) is safe and does not touch call sites, overload resolution, or any registration/arithmetic order. No numerical or Ceres-ordering code lives in this file.
    - [naming/none/trivial] L21: Rename parameter `frames` to `frame` — the type is a single std::shared_ptr<Frame>, and the .cpp definition already uses `frame` for this parameter, so the header is currently inconsistent with its own implementation.
    - [naming/none/trivial] L30: Fix the typo `marignal_keyframes` -> `marginal_keyframes` (or align with the .cpp's `marginal_kf_ids`) for clarity and consistency with the definition.
    - [comment/none/trivial] L16: This Doxygen block is an empty stub: @brief has no text and @param frame doesn't match the actual parameter name (`frames`/proposed `frame`). Either fill in a real description of what single-frame optimization does and its parameters, or remove the stub comment entirely so it isn't misleading.
    - [local-structure/none/trivial] L34: Merge into a single `private:` section listing the method then the member, removing the redundant second access-specifier label.

## odometry  (6 files, 33 findings)

- **cpp/src/odometry/imu_preintegration.cpp** (271 loc)
    - _risk_: This is dense linear-algebra code where the exact sequence of matrix multiplications and additions determines floating-point rounding (jacobian_ = F * jacobian_, covariance_ = F*covariance_*F^T + V*Q*V^T, and the symmetrization step). I deliberately did NOT propose collapsing the algebraically-related dp/dv block pairs (e.g. F.block<3,3>(0,3) equals 0.5*dt_sec times F.block<3,3>(6,3) in exact arithmetic) into a shared computation, because doing so would change the floating-point operation sequence/rounding even though the real-number result is identical — this violates the prime directive despite looking like safe duplication removal. Any touch to propagate_error's block assignments (lines 169-224) must preserve each block's independent literal expression and evaluation order. The propagate_error parameter rename (R_start/R_next) requires a matching edit in the header (imu_preintegration.hpp:129-130) since it's a private method declared there.
    - [local-structure/none/trivial] L28: Reorder the mem-initializer list (lines 28-39) to match the declaration order in imu_preintegration.hpp (delta_p_ before delta_r_ before delta_v_) to avoid a -Wreorder warning and match actual init order. Each of these initializations is independent (no cross-dependency), so real execution order and values are unaffected — only the textual list order changes.
    - [naming/none/small] L152: Rename parameters R_start/R_next to r_start/r_next (or rot_start/rot_next) to match this file's snake_case convention used everywhere else (e.g. delta_r_start, acc_world_mid). Requires the matching private-method declaration in imu_preintegration.hpp:129-130 to be updated too; call site at lines 102-107 passes anonymous .matrix() expressions so no caller-side renames are needed.
    - [naming/none/trivial] L162: Rename tmp to something that names what it represents, e.g. rot_step_jacobian_approx or gyro_linearization_term, and add a short comment noting it's the first-order (I - [w]_x*dt) approximation of the rotation-step Jacobian reused in the dtheta, dv, and dp blocks below (lines 170, 174, 179).
    - [helper-extraction/none/trivial] L207: Extract a small local lambda, e.g. auto square = [](double v) { return v * v; };, and use square(parameters_.acc_noise_sigma) etc. Removes the repeated *x*x* pattern without changing any operation order or values (squaring a scalar is order-independent).
    - [comment/none/trivial] L239: Add a short why-comment above the damping line explaining that the diagonal regularization guards LDLT against a singular/near-singular covariance (e.g. right after reset or with very few integration steps), since this isn't otherwise obvious from the code.
- **cpp/src/odometry/imu_preintegration.hpp** (160 loc)
    - _risk_: This is a declarations-only header; ImuPreintegration and InertialState are consumed across files (e.g. optimizer/vio_estimator.cpp), so any public method/member rename here would be a cross-file public rename and is explicitly out of scope for this pass. InertialState::set_lin_true()/is_linearized()/*_lin() intentionally mirror Frame's identical FEJ pattern (frame.hpp) — do not rename independently of that pattern. The `information()` default damping (1e-12) only appears in this header; the .cpp definition takes no default, so naming the constant must stay in this file to keep the effective default unchanged.
    - [comment/none/trivial] L154: Update the stale PascalCase method references (IntegrateMeasurement(s), Repropagate) to the actual snake_case names integrate_measurement(s) and repropagate(), which is what these comments should be pointing readers to.
    - [magic-number/none/trivial] L100: Introduce a named constexpr (e.g. static constexpr double kDefaultInformationDamping = 1e-12;) in the class and use it as the default argument instead of the bare literal, so the regularization floor is self-documenting. Same value, no behavior change.
    - [comment/none/trivial] L79: Add a short comment noting that bias() only overwrites bias_acc_/bias_gyr_ (unlike reset(), which also clears delta_p_/delta_r_/delta_v_/jacobian_/covariance_ and the sample buffer), since the two setters share an identical signature and are easy to confuse.
- **cpp/src/odometry/sliding_window.cpp** (149 loc)
    - _risk_: remove_frames() drives Ceres marginalization bookkeeping (which frames/map-points survive the window) — the two prune_container passes over map_points_ and map_point_candidates_ must keep running in the same order (map_points_ then map_point_candidates_), the erase-during-iteration pattern must be preserved exactly, and the final cleanup loop over frames_ must still run after ids are erased from frames_/frame_ids_/keyframe_ids_. Any 'local-structure' edit here needs to leave iteration/erase order and the sequencing of the three loops untouched.
    - [dead-code/none/trivial] L37: Remove this unused local; it is assigned but never read before frame_ids_.insert(id)/frames_.emplace(id, frame) run. Looks like leftover from a prior existence-check that was refactored away.
    - [dead-code/none/trivial] L116: Remove this unused local in add_map_point; it is never read before the subsequent map_points_.emplace(id, map_point) call.
    - [comment/none/trivial] L59: Add a brief comment above the lambda stating the pruning contract: an entry is dropped entirely if its shared_ptr is null, if its host frame is being removed, or if removing the given frame ids empties its observation set; otherwise only the stale observations are erased. This isn't obvious from the code alone and the lambda is reused for both map_points_ and map_point_candidates_.
- **cpp/src/odometry/sliding_window.hpp** (62 loc) — clean
- **cpp/src/odometry/stereo_vio.cpp** (822 loc)
    - _risk_: inertial_states_/imu_preintegrations_/created_map_point_nums_ are ordered std::map<uint64_t,...> (deterministic ascending-id iteration), but sliding_window_->map_points()/map_point_candidates() are std::unordered_map<uint64_t, shared_ptr<MapPoint>> (cpp/src/odometry/sliding_window.hpp) — iteration order there is insertion/bucket-dependent, not key-sorted. Any touch to initialize_map_points() (loop over candidates, erase_mp_ids insertion/erasure order) or build_odometry_result() (loop over map_points) must preserve the existing iteration order exactly, since it drives the order points are added to the sliding window / appended into result vectors, which can feed Ceres parameter-block registration order elsewhere. The keyframe-marginalization selection loop (select_marginal_frames, ~lines 601-721) has intricate ordering/tie-break logic (std::set iteration is key-sorted, `kf_ids.erase` during the while-loop, first-eligible-wins semantics) — do not reorder loop bounds or comparison directions even for a "clarity" refactor.
    - [naming/none/trivial] L433: Rename to `cam_num` (lowercase). This is a per-call runtime value from frame->cam_num(), but it is named identically to the class's `static constexpr size_t kCamNum = 2;` (cpp/include/omni_slam/stereo_vio.hpp:68) used elsewhere in this same file (e.g. line 85) — the shared name falsely implies a compile-time constant and can mislead readers into thinking it's always 2.
    - [comment/none/trivial] L258: Reword to describe what actually happens: the latest IMU sample in the batch (imu_data.back()) is used as the gravity reference, not the first sample. Update the comment text only.
    - [dead-code/none/trivial] L512: Remove the unused `success` local from initialize_map_points; it is always false and never observed after the loop.
    - [dead-code/none/trivial] L552: This file already uses an active `LogD(...)` debug-logger call elsewhere (line 471). Either re-enable this one the same way, or drop it along with the now write-only `old_count`/`try_count` counters (lines 494-495, 505) if it's no longer useful.
    - [dead-code/none/trivial] L391: The join_ids lambda's only call sites are commented out, making the whole block dead. Convert to real `LogD(...)` debug-guarded calls (matching the pattern already used in this file) or remove the block entirely.
    - [dead-code/none/trivial] L357: This looks like obsolete/disabled scaffolding superseded by the optimize_window() call below. Remove it if OptimizeSingleFrame is no longer a supported path, or add a short comment explaining why it's intentionally kept disabled.
    - [dead-code/none/trivial] L183: Leftover commented-out profiling call. Remove if obsolete, or gate behind an explicit debug/profiling compile flag if it's still wanted occasionally.
    - [comment/none/trivial] L588: Renumber the phase comments sequentially (1, 2, 3) or add a note about what happened to the missing phase, so the numbering doesn't read as an accidentally-deleted step.
    - [naming/none/trivial] L630: Rename to something that states what the iterator marks (e.g. `marginalizable_kf_end` or `eligible_kf_end`) rather than how it is computed; the current name reads like an arithmetic expression rather than a meaningful boundary.
    - [magic-number/none/trivial] L535: Extract `3.0` into a named constexpr (e.g. `constexpr double kMaxInverseDepth = 3.0;`) with a short comment on what it represents (an upper bound on inverse depth, i.e. a minimum depth of ~0.33 m).
    - [magic-number/none/trivial] L693: Extract `1e-5` into a named constexpr (e.g. `constexpr double kDistEpsilon = 1e-5;`) documenting that it guards against divide-by-zero when two keyframe positions coincide.
    - [magic-number/none/trivial] L52: Name this initializer (e.g. `constexpr int kInitialNewKeyframeAfter = 100;`) with a brief comment on why it starts large (acts as a sentinel prior to the first configured comparison / before setup() runs).
    - [helper-extraction/low/medium] L667: Extract a small local lambda, e.g. `compute_marginalization_score(candidate_id)`, capturing `kf_ids`, `end_minus_inertial_states`, `last_kf_id`, and `sliding_window_`, called in the exact same iteration order as today, to name the heuristic and shrink the surrounding while-loop body without changing any arithmetic or iteration order.
- **cpp/src/odometry/stereo_vo.cpp** (585 loc)
    - _risk_: initialize_map_points() breaks out of its inner loop on the first triangulation success while iterating `frame_id_to_bearing`, which is a std::unordered_map<FrameCamId, Eigen::Vector3d> (see map_point.hpp) — its iteration order is unspecified, so which co-observing frame ends up hosting a new map point is order-dependent today. Any touch-up near that loop (lines 319-361) must not add sorting, filtering, or restructuring that could change which element is visited first. select_marginal_frames() accumulates `denom` and `score` in nested loops (lines 449-480) whose float/double arithmetic order must stay exactly as written since it feeds a min-score comparison. Do not reorder the ScopedTimer-wrapped blocks in track() (optimize_single_frame -> optimize_window -> select/remove -> marginalize -> remove keyframe) since Ceres block registration and marginalization correctness depend on that sequence.
    - [naming/none/trivial] L251: Rename this local to something like `cam_num` (or `frame_cam_num`). It shadows the class's static constexpr StereoVO::kCamNum (stereo_vo.hpp, used at line 47) with a per-frame runtime value, and the 'k' prefix conventionally signals a compile-time constant elsewhere in this codebase, which is misleading here.
    - [dead-code/none/trivial] L433: Remove this unused local; the very next if-condition (lines 435-437) recomputes the same ratio independently via float(count)/float(created), so `ratio` is dead computation with no observable effect.
    - [magic-number/none/trivial] L350: Extract 3.0 to a named constexpr (e.g. `static constexpr double kMaxTriangulatedInvDist = 3.0;`) documenting it as the inverse-distance upper bound accepted for a newly triangulated map point.
    - [magic-number/none/trivial] L464: Extract 1e-5 to a named constexpr (e.g. `kMinPairwiseDistanceEpsilon`) documenting that it guards the reciprocal-distance sum against division by zero when two keyframes coincide.
    - [magic-number/none/small] L420: Extract the literal 2 (used identically at lines 420 and 427) to a named constexpr, e.g. `constexpr size_t kProtectedRecentKeyframes = 2;`, documenting that the two most recent keyframes are always excluded from marginalization selection.
    - [comment/none/trivial] L25: Add a comment explaining why 100 is used as the initial counter value (an arbitrary large sentinel intended to be greater than SVOConfig::new_keyframe_after so the very first tracked frame always attempts a keyframe check); the assumption is not obvious from the call site in track()/initialize().
    - [local-structure/none/trivial] L262: Declare as `const size_t point_num = tracking_result->size(i);` instead of binding a const reference to the temporary returned by size() (which returns size_t by value); avoids the reference-to-temporary idiom for a plain value.
    - [naming/none/small] L312: Rename `old_count` to something like `stale_candidate_count` (candidates whose host frame lacks an observation in this frame's cam0) and `try_count` to `initial_candidate_count`, to make their meaning clear without re-reading the loop body.
    - [local-structure/none/trivial] L268: Use the already-bound `id` alias instead of re-indexing `ids[j]` on the next line, for consistency and to avoid the redundant duplicate lookup expression.

## include  (5 files, 3 findings)

- **cpp/include/omni_slam/odometry.hpp** (16 loc) — clean
- **cpp/include/omni_slam/odometry_result.hpp** (34 loc) — clean
- **cpp/include/omni_slam/stereo_vio.hpp** (102 loc)
    - _risk_: This is a declarations-only header (no arithmetic or Ceres/marginalization code), so there is essentially no floating-point or ordering risk in this file itself. The one thing to watch: `imu_parameters` (no trailing underscore, line 98) is referenced by name in the .cpp constructor/init list and elsewhere, so any rename must be applied consistently across cpp/src/odometry/stereo_vio.cpp in the same change to avoid a build break — it is otherwise a private member, not a public API, so it is in-scope for this pass.
    - [naming/low/small] L98: Rename to imu_parameters_ to match the trailing-underscore convention used by every other private member in this class (status_, running_, sliding_window_, etc.); update the corresponding uses in cpp/src/odometry/stereo_vio.cpp (member-init list and ~5 call sites) in the same change.
    - [dead-code/none/trivial] L24: Forward declaration is unused in this header (no method/member of StereoVIO references TrackingResult); the sole includer, cpp/src/odometry/stereo_vio.cpp, obtains the full type through its own includes (e.g. database/frame.hpp) rather than relying on this forward decl. Safe to remove as unused scaffolding.
    - [dead-code/none/trivial] L27: Forward declaration is unused in this header (no method/member references MapPoint by name); cpp/src/odometry/stereo_vio.cpp already includes database/map_point.hpp directly for the full definition. Safe to remove as unused scaffolding.
- **cpp/include/omni_slam/stereo_vo.hpp** (83 loc) — clean
- **cpp/include/omni_slam/types.hpp** (62 loc)
    - _risk_: Any rename of CameraParameter/FrameCamId members would be a public rename (out of scope, used across TUs). FrameCamId's field order/types feed operator== and the hash combine (h1 ^ (h2 << 1)); do not reorder or retype without checking every construction site.

## apps  (2 files, 12 findings)

- **apps/run_svio.cpp** (375 loc)
    - _risk_: Any extraction touching the origins/vectors/colors/radii buffers (log_origin_axes lines 102-135, and the per-frame body-axes block lines 311-364) must preserve the exact push order (origin, vector, color, radius; x-axis then y-axis then z-axis) since Rerun index-aligns these parallel arrays into rows — reordering pushes would silently swap which color/radius applies to which arrow. Any extraction in make_rerun_image must preserve the existing type-check order (CV_8UC1 before CV_8UC3, source before the convertTo fallback) since that order encodes the fallback behavior for unrecognized formats.
    - [dead-code/none/trivial] L145: This commented-out alternate dataset path is obsolete scaffolding, not debug output worth preserving. Remove it, or if the alternate dataset is still relevant, replace it with a short prose comment noting other datasets can be substituted here (rather than dead, stale code).
    - [helper-extraction/low/medium] L53: Extract a local lambda, e.g. `auto to_image = [width, height](const cv::Mat& mat, rerun::Collection<uint8_t> data) -> rerun::Image { ... same two if-checks ... };` and call it once for `source`/`bytes` and once for `converted`/`converted_bytes`, preserving the existing check order (CV_8UC1 before CV_8UC3) and the final clear_fields() fallback.
    - [helper-extraction/low/small] L334: Extract a small local lambda within the enclosing scope, e.g. `auto push_axis = [&](const Eigen::Vector3d& axis_vec, const rerun::components::Color& color) { origins.push_back(origin); vectors.emplace_back(static_cast<float>(axis_vec.x()), static_cast<float>(axis_vec.y()), static_cast<float>(axis_vec.z())); colors.emplace_back(color); radii.emplace_back(rerun::components::Radius::scene_units(kAxisRadius)); };` then call `push_axis(x, kAxisXColor); push_axis(y, kAxisYColor); push_axis(z, kAxisZColor);` in that exact order to remove the triplication without changing push order.
    - [magic-number/none/trivial] L366: Introduce a named constexpr (e.g. `constexpr auto kResultPollInterval = std::chrono::milliseconds(10);` alongside the other UI constants) and use it here, documenting that it's the result-polling cadence.
    - [naming/none/trivial] L264: Rename `packed` to `map_point` (or `point`) — "packed" doesn't convey what the variable represents and there's no packing happening at this call site.
- **apps/run_svo.cpp** (356 loc)
    - _risk_: This is a visualization/demo app, not the core estimator, so most findings here are low risk with respect to the "no behavior change" directive since they touch logging/UI code rather than optimization math. Still must preserve: (1) the exact order of rec.log(...) calls (rerun ingests by call order and uses set_time_sequence/set_time_timestamp_nanos_since_epoch state, so reordering or merging log calls could change which timeline sample data lands on), (2) the exact sequence in which values are pushed into the parallel vectors (origins/vectors/colors/radii and points2d/radii/colors) since rerun zips these vectors positionally, (3) which branch (CV_8UC1 vs CV_8UC3 vs convert-then-branch) currently executes for a given cv::Mat type in make_rerun_image — any consolidation must keep the exact same type-dispatch outcome.
    - [dead-code/none/trivial] L145: Remove the commented-out alternate dataset path line (or replace with a single short comment noting other datasets can be substituted here); this is stale scaffolding, not debug output worth preserving behind a logger.
    - [helper-extraction/low/small] L53: Extract a local static helper, e.g. `rerun::Image dispatch_by_type(const cv::Mat& m, uint32_t width, uint32_t height)`, containing the CV_8UC1/CV_8UC3 branching, and call it once for `source` and once for `converted`, preserving the exact same fallback order (try source as-is, else convertTo(CV_8U) and retry).
    - [helper-extraction/low/small] L113: Extract a small local lambda, e.g. `auto append_axis = [&](const Position3D& origin, const Vector3D& v, const Color& c, float radius) { origins.push_back(origin); vectors.push_back(v); colors.push_back(c); radii.emplace_back(Radius::scene_units(radius)); };`, and call it 3 times in each site in the same X,Y,Z order, removing the duplicated push_back sequences without changing what gets appended or in what order.
    - [naming/none/trivial] L254: Rename loop variable `packed` to `map_point` (or `point`) to describe what it represents rather than an implementation detail.
    - [magic-number/none/trivial] L347: Introduce `constexpr auto kResultPollInterval = std::chrono::milliseconds(10);` near the other UI constants and use it here, documenting that this is the fetch_result polling cadence.
    - [local-structure/none/trivial] L298: Introduce a named intermediate `const size_t axis_component_count = result.T_w_b_window.size() * 3;` and reuse it in the four reserve() calls instead of recomputing the same expression four times.
    - [helper-extraction/low/medium] L274: Extract each into a small named local lambda (e.g. `log_window_and_camera_poses(...)`, `log_body_axes(...)`) defined just above the main loop and invoked in the same place/order, to shorten the already-long per-result block and let each step's name convey intent without altering call order.

